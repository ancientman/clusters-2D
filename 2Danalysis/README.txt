CoreFunctions\starterFun.m starts the pipeline.

The function takes the folder containing expInfo.txt as the input variable.
It then constructs a metadata file by reading the paramters in expInfo.txt and proceeds with the main function.